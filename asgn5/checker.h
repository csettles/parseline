#ifndef CHECKER_H
#define CHECKER_H

int handle_line(char *input, int stagno);
int handle_input(char *input, int stageno);
int handle_output(char *input, int stageno);
int handle_args(char *input, int stageno);

#endif
